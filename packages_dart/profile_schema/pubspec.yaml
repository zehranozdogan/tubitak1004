name: profile_schema
description: >
  packages/profile_schema (Python) JSON okuma/doğrulamasının Dart portu —
  sensor_profile, layout_version, label_payload için elle yazılmış
  (jsonschema kütüphanesi olmadan) tip-güvenli doğrulama.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.13.4

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
