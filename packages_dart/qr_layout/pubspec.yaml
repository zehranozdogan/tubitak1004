name: qr_layout
description: >
  packages/qr_layout (Python) reaktif hücre yerleşimi + QR geometrisinin
  Dart portu. Gerçek QR encoding (segno) YOK — sadece ISO/IEC 18004
  geometrisi ve proje-özel reaktif hücre seçim mantığı.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.13.4

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
